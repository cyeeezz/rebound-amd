# Secrets — never commit API keys.
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# Rebound backups / scratch
app.py.*-backup
requirements.txt.*-backup
backup/

# OS / editor cruft
.DS_Store
.idea/
.vscode/

# NOTE: amd_artifacts/ and notebooks/ ARE committed — they are verified
# hackathon evidence and a runtime product feature (loaded via utils.amd_loader).
